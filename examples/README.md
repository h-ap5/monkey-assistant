# Example files | 예제 파일

The two `.user.js` fixtures contain identical userscript code under different filenames. Import both files to verify that Script Audit Lab reports one exact-duplicate group.

두 `.user.js` 예제는 파일명만 다르고 내부 코드는 같습니다. 두 파일을 함께 불러오면 Script Audit Lab이 완전 중복 그룹 한 건을 표시하는지 확인할 수 있습니다.
